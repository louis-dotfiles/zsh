# Plugins directory

This is where you can define plugins and their specific options.

The plugins themselves are specified in the [list](./list.txt).
[Antidote](https://antidote.sh/), the plugin manager currently in use, will manage the installation
and start the plugins.

The plugin configurations are scattered in different files.
